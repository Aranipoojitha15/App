1. Clone from git
2. open a terminal in VS Code 
3. go to the server directory and use the commands
     - npm i --force
     - npm start
4. open a new terminal in VS Code and go to the MovieApp directory and use the commands
      - npm i --force
      - npm start
